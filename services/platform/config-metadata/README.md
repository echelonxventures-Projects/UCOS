# services/platform/config-metadata/

**Reserved for `WP-PLT-11` — Configuration & Metadata Delivery (PE-11).**

> EMPTY BY DESIGN (structure only). Established by **WI-SEED.1 (Repository Foundation)**, Phase 11.3.
> No service code is generated by the seed. Service implementation is authorized only when `WP-PLT-11`
> commences at implementation-sequence step [5] (`UCOS-IMP-KICK-PI1-001` §5).

When populated, this service MUST:
- realize only the ratified published contract `UCOS-API-CONTRACT-018` (+ `UCOS-EVT-CONTRACT-018`,
  `UCOS-DATA-CONTRACT-018`) — contract-first (IC-2);
- conform to **ADR-005** (PostgreSQL SoR + Config/Metadata API + GitOps + JSON Schema; **secrets excluded**);
- enforce non-waivable **S1** on its boundary and **S3** secrets discipline (SEC-CTL-001/002/014, 005);
- register in `CTX-REG-001` with full traceability (IC-4).

**Traceability:** `PEA-001` PE-11 · `PEA-005/006` · ADR-005 · CAP-10 · DOM-018 · `PRS-043..046` · `MC-01..13`.
